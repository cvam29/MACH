namespace Mach.Persistence;

/// <summary>
/// Skeleton marker for the Persistence layer. TODO (Wave 1a): EF Core DbContext,
/// schema-per-concern tables, migrations, and the outbox/idempotency/projection stores.
/// Provides a public anchor type for assembly-loading in architecture tests.
/// </summary>
public static class PlaceholderMarker;
